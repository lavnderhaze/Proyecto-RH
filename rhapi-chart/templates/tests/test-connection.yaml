apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rhapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rhapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rhapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
